#include "dod.h"
#include <math.h>
#include "var.h" // підключили зовнішні оголошення змінних
using namespace nsVar;
void nsDod::dod() {
	a *= ((pow((-1),n+1)) / ((2 * n + 1) * x)); // доданок домножується на
}
